# Development and CI dependencies. Install with: make setup-dev
-r requirements.txt

pytest==8.3.3
httpx2==2.12.0         # transport used by fastapi.testclient
ruff==0.6.9            # linter and formatter
pip-audit==2.10.1      # dependency vulnerability scan
