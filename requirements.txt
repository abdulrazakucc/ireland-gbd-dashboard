# Runtime dependencies, pinned so every environment is identical.
# Install with: make setup
fastapi==0.141.1
uvicorn[standard]==0.52.4
pandas==3.0.5
matplotlib==3.9.2    # PNG/PDF figure export (app/figures.py)
